# Expert TypeScript baseline, beginner functional-programming baseline

The learner reports top-tier TypeScript fluency and only meme-level familiarity with functional programming. Future lessons can move quickly through types and syntax, but must derive Effect concepts from operational behavior before naming FP abstractions.

## Implications

Use Dumgen's real Promise APIs as the bridge into Effect; do not front-load monads, monoids, or typeclass vocabulary.
