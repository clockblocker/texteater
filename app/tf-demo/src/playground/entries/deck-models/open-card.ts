/**
 * Kept as a re-export: `OPEN_SCALE` now lives in `motion-spec.ts` with the
 * rest of the playground's motion, so there is one file to change. Import
 * it from there in new code.
 */
export { OPEN_SCALE } from "./motion-spec";
