import { CompassModel } from "./deck-models/drag-deck";

/** The full reader uses the same renderer, gestures, and motion policy as the workbench. */
export function DeckModelsGallery() {
	return <CompassModel />;
}
