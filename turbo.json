{
	"$schema": "https://turborepo.dev/schema.json",
	"tasks": {
		"build:package": {
			"dependsOn": ["^build:package"],
			"outputs": ["dist/**"]
		},
		"dev:package": {
			"dependsOn": ["^build:package"],
			"cache": false,
			"persistent": true
		}
	}
}
